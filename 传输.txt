Now I understand the problem completely. Let me be direct.The numbers are unambiguous. Let me write the honest diagnosis and the real fix.直接说结论：

**事件式信号处理方案在你的完整留出集上失败了，主因是 Phase 1（峰检测）不够鲁棒。** 在 16 段 GT 中，oracle key_recall 最高只有 0.60，均值 0.375 —— 检测器漏掉了 62% 的真实按键。分组和打分再好也无法弥补。我之前的好数字（eval IoU 0.903）确实只在我 sandbox 里那 2 条测试上成立。

**但 IKI 这个特征本身是真的、经数据验证的。** password IKI 中位数 = 1.448s，free typing IKI 中位数 = 0.079s，差异 18 倍。这不是我推测的，是从你 401 个真实 IKI 间隔算出来的。

**正确的使用方式不是替换你的 dense labeling，而是在它上面叠加 IKI 后过滤。** 具体：

1. 你现有 dense labeling 的候选段已经有很好的 recall（oracle IoU 0.82-0.86）
2. 你现有的 keycount>=7 过滤能把 len9 retry 从 6.3 段压到 2.0 段，但会**伤害**老 p01（oracle IoU 从 0.818 掉到 0.670，key_recall 从 0.906 掉到 0.750）
3. IKI 过滤比 keycount 更优：它不仅能删掉 free typing 误报（IKI < 0.25s），还能**保留被切碎的 password 片段**——因为即使只剩 5 个 key，它们的 IKI 仍然是 ~1.5s，通过 IKI 检验

`stage1_dense_plus_iki_filter.py` 提供了 `iki_filter_segments()` 和 `filter_dense_labeling_output()`，可以直接 import 到你现有的 eval 代码里，3 行接入。