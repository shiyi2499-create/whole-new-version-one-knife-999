# Keystroke Vibration Data Collector - Dependencies
# Install: pip install -r requirements.txt

# Apple Silicon IMU access
# - direct AppleSPUHIDDevice backend is built into spu_backend.py (no extra dep)
# - macimu is kept for optional legacy/root fallback
macimu

# Keyboard event listener (cross-platform)
pynput

# Numerical processing (for preprocessor)
numpy
scipy
tqdm
